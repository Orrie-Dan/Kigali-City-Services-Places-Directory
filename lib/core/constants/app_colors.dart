import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand / accent colors
  static const Color primary = Color(0xFFFFC857); // warm amber
  static const Color primaryVariant = Color(0xFFFFA000);

  // Surfaces
  static const Color background = Color(0xFFF5F7FB);
  static const Color surface = Colors.white;

  // Typography
  static const Color textPrimary = Color(0xFF101626);
  static const Color textSecondary = Colors.black54;

  // Navigation
  static const Color navBackground = Color(0xFF071833);
  static const Color navActive = Colors.white;
  static const Color navInactive = Colors.white60;

  static const Color error = Colors.redAccent;
}

